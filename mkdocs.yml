site_name: MkLorum
site_url: https://example.com/

nav:
    - Home: index.md
    - About: about.md
    - Models: models.md

theme: readthedocs

plugins:
  - mkdocstrings:
      handlers:
        python:
          paths: [src]  # search packages in the src folder
